language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ashaydukov/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ashaydukov/docker-react npm run test
